<UserControl x:Class="Lab_03.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab_03.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_New Question Pack" Command="{Binding OpenAddQuestionPackDialogCommand}"/>
            <MenuItem Header="Select Question Pack" ItemsSource = "{Binding packs}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" Value="{Binding Name}" />
                        <Setter Property="Command" Value="{Binding DataContext.SetActivePackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                        <Setter Property="CommandParameter" Value="{Binding}" />
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            <MenuItem Header="Delete Question Pack" Command="{Binding DeleteQuestionPackCommand}" />
            <Separator />
            <MenuItem Header="Import Questions" />
            <Separator />
            <MenuItem Header="E_xit" />
        </MenuItem>
        <MenuItem Header="Edit">
            <MenuItem Header="Add Question" Command="{Binding ConfigurationViewModel.AddQuestionCommand}"/>
            <MenuItem Header="Remove Question" Command="{Binding ConfigurationViewModel.RemoveQuestionCommand}"/>
            <MenuItem Header="Pack Options" />
        </MenuItem>
        <MenuItem Header="View">
            <MenuItem Header="Play" />
            <MenuItem Header="Edit" />
            <MenuItem Header="Fullscreen" />
        </MenuItem>
    </Menu>
</UserControl>
