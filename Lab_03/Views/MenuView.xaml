<UserControl x:Class="Lab_03.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab_03.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_New Question Pack" />
            <MenuItem Header="Select Question Pack" 
                      Command="{Binding ConfigurationViewModel.SetActivePackCommand}"
                      ItemsSource="{Binding ConfigurationViewModel.AllPacks}"
                      />
            <MenuItem Header="Delete Question Pack" />
            <Separator />
            <MenuItem Header="Import Questions" />
            <Separator />
            <MenuItem Header="E_xit" />
        </MenuItem>
        <MenuItem Header="Edit">
            <MenuItem Header="Add Question" />
            <MenuItem Header="Remove Question" />
            <MenuItem Header="Pack Options" />
        </MenuItem>
        <MenuItem Header="View">
            <MenuItem Header="Play" />
            <MenuItem Header="Edit" />
            <MenuItem Header="Fullscreen" />
        </MenuItem>
    </Menu>
</UserControl>
